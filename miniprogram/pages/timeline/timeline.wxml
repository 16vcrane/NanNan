<view class="page">
  <view class="page-title">时光</view>
  <view class="page-subtitle">往日记录会在这里按时间呈现</view>

  <view wx:if="{{loading}}" class="timeline-state">
    <view class="loading-line"></view>
    <text class="muted">正在翻开时光</text>
  </view>

  <view wx:elif="{{loadError}}" class="timeline-state">
    <text class="empty-title">暂时没能打开时光</text>
    <button class="retry-action" bindtap="loadDiaries">重新加载</button>
  </view>

  <view wx:elif="{{!diaries.length}}" class="surface empty-state">
    <text class="empty-title">还没有日记</text>
    <text class="muted">写下第一篇记录后，它会出现在这里</text>
    <button class="write-action" bindtap="goToToday">去记录此刻</button>
  </view>

  <view wx:else class="timeline-list">
    <view wx:for="{{diaries}}" wx:key="id" class="timeline-entry">
      <view class="timeline-rail">
        <view class="timeline-dot"></view>
        <view class="timeline-line"></view>
      </view>
      <view class="diary-card">
        <view class="card-meta">
          <text class="date-text">{{item.dateText}}</text>
          <text class="mood-text">{{item.moodText}}</text>
        </view>
        <text class="diary-summary">{{item.summary}}</text>
        <view wx:if="{{item.markers.length}}" class="marker-list">
          <view
            wx:for="{{item.markers}}"
            wx:for-item="marker"
            wx:key="id"
            class="marker"
            style="border-color: {{marker.color}}; color: {{marker.color}};"
          >
            <view class="marker-pin" style="background: {{marker.color}};"></view>
            <text>{{marker.displayText}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
